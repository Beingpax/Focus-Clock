{
  "name": "focus-clock",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.121.0"
  }
}
